[package]
name = "fluxcell-sdk"
version = "0.1.0"
edition = "2024"
description = "Canonical Guest Software Development Kit for SpectraFlux WebAssembly Fluxcells"
license = "MIT"
authors = ["SpectraGQL Contributors"]

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
